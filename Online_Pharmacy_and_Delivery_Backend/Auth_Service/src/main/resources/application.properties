spring.application.name=auth-service
spring.main.lazy-initialization=true
server.port=8081
server.tomcat.threads.max=10

spring.datasource.url=jdbc:mysql://localhost:3306/usersdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.hikari.maximum-pool-size=${DB_MAX_POOL_SIZE:5}
spring.datasource.hikari.minimum-idle=${DB_MIN_IDLE:1}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:30000}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.open-in-view=false

jwt.secret=${JWT_SECRET:qBc8HQhVL9u+uBUPltUg8arrAZFVcSWDocPzkWmAKmc=}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
jwt.refresh-expiration-ms=${JWT_REFRESH_EXPIRATION_MS:604800000}

springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.config-url=/api-docs/swagger-config
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=${EUREKA_SERVER_URL:http://${EUREKA_USERNAME:eureka}:${EUREKA_PASSWORD:eureka}@localhost:8761/eureka}

management.tracing.sampling.probability=${TRACING_SAMPLING_PROBABILITY:1.0}
management.zipkin.tracing.endpoint=${ZIPKIN_ENDPOINT:http://localhost:9411/api/v2/spans}
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when-authorized
management.endpoint.health.roles=ADMIN
management.endpoint.prometheus.enabled=true
management.prometheus.metrics.export.enabled=true
management.metrics.tags.application=${spring.application.name}
management.metrics.distribution.percentiles-histogram.http.server.requests=true

observability.loki.enabled=${LOKI_ENABLED:true}
observability.loki.url=${LOKI_URL:http://localhost:3100/loki/api/v1/push}

logging.level.org.sprint.authService=${APP_LOG_LEVEL:INFO}
logging.level.root=ERROR
logging.level.org.springframework=ERROR

jwt.revocation.enabled=${JWT_REVOCATION_ENABLED:true}
jwt.revocation.jti-prefix=${JWT_REVOCATION_JTI_PREFIX:jwt:revoked:jti:}
jwt.revocation.hash-prefix=${JWT_REVOCATION_HASH_PREFIX:jwt:revoked:hash:}

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=${REDIS_TIMEOUT:2s}

spring.data.redis.repositories.enabled=false

app.seed.enabled=${APP_SEED_ENABLED:true}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

pharmacy.events.exchange=${PHARMACY_EVENTS_EXCHANGE:pharmacy.events.exchange}
pharmacy.events.rabbit.enabled=${PHARMACY_EVENTS_ENABLED:true}

otp.expiration-minutes=${OTP_EXPIRATION_MINUTES:10}
otp.max-attempts=${OTP_MAX_ATTEMPTS:3}
otp.length=${OTP_LENGTH:6}
otp.rate-limit-max=${OTP_RATE_LIMIT_MAX:3}
otp.rate-limit-window-minutes=${OTP_RATE_LIMIT_WINDOW:60}

verification.token.expiration-hours=${VERIFICATION_TOKEN_EXPIRATION_HOURS:24}
app.base-url=${APP_BASE_URL:http://localhost:8081}
app.frontend-base-url=${APP_FRONTEND_BASE_URL:http://localhost:5173}

password.reset.expiration-minutes=${PASSWORD_RESET_EXPIRATION_MINUTES:60}
